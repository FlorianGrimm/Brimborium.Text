using System.Collections.Generic;

namespace Brimborium.Text;

public class TokenizedTextTests {

    /*
    [Fact]
    public void Constructor_InitializesProperties() {
        // Arrange
        var text = new StringSlice("test text");
        var tokens = new List<ITokenStringSlice<string>>();
        tokens.Add(new TokenStringSlice<string>("test", new StringSlice("test text", 0..4)));
        tokens.Add(new TokenStringSlice<string>("text", new StringSlice("test text", 5..9)));

        // Act
        var tokenizedText = new TokenizedText<string>(text, tokens);

        // Assert
        Assert.Same(text, tokenizedText.Text);
        Assert.Same(tokens, tokenizedText.ListTokens);
        Assert.Equal(2, tokenizedText.ListTokens.Count);
        Assert.Equal("test", tokenizedText.ListTokens[0].Token);
        Assert.Equal("text", tokenizedText.ListTokens[1].Token);
    }
    */
}